AI-Based Personalized Cosmetic Product and Home Remedy Recommendation System
pip freeze > requirements.txt

requiremrnts wants to install than use file name "r1.txt" command is = pip install -r r1.txt

for run d:/ayuraAI/.venv/Scripts/python.exe d:/ayuraAI/app.py
for install package : pip install streamlit pandas numpy openai google-genai requests python-dotenv pillow Authlib cryptography GitPython tqdm altair pyarrow